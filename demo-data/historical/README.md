# Historical Payment Records

Prior payment outcomes used by the historical risk analysis in the
BeforePaymentSubmissionAgent.

Content is evidence-only: prior payments, prior returns, recurring reason
codes (for example R01), customer-level and beneficiary-level rejection
trends.

Real fixtures are added in a later prompt.
